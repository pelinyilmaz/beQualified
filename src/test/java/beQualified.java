import io.github.bonigarcia.wdm.WebDriverManager;import org.openqa.selenium.By;import org.openqa.selenium.Keys;import org.openqa.selenium.WebDriver;import org.openqa.selenium.chrome.ChromeDriver;import org.openqa.selenium.support.ui.ExpectedConditions;import org.openqa.selenium.support.ui.WebDriverWait;public class beQualified {    public static void main(String[] args ){        int sumResult = 5;        String keyServey = null;        if (args.length == 1) {            keyServey = args[0];            driver(keyServey, sumResult);        } else {            System.out.println("Subject is absence");            System.exit(1);        }    }    public static void driver(String keyServey, int sumResult) {        //          1 --> Set up needed driver        WebDriverManager.chromedriver().setup();        WebDriver driver = new ChromeDriver();        WebDriverWait wait = new WebDriverWait(driver, 7);        //          2 --> Navigate to WebBrowser        driver.get("https://www.google.com/");        //          3 --> Locating the search box and then send given arguments        driver.findElement(By.name("q")).sendKeys(keyServey + Keys.ENTER);        //          4 --> Mesh/Synchronize the needed web page till the title to be founded        wait.until(ExpectedConditions.titleContains(keyServey));        //          5 --> Print WebElement(located one)        String arguments = driver.findElement(By.id("result-stats")).getText();        //          6 --> Last put the WebElement in iteration to carrying through the first five results        System.out.println("\n=================================================="                + "\n\t GIVEN CONTROVERSIAL = " + keyServey                + "\n\t RESULT NUMERICS = " + sumResult + "\n");        System.out.println(arguments + "\n======================================");        for (int x = 1; x <=6; x++) {            String path = "(//h3/..)[" + x + "]";            String linkText = driver.findElement(By.xpath(path)).getAttribute("href");            if(linkText!=null ){                System.out.println("- " + linkText);            }        }        driver.quit();    }}